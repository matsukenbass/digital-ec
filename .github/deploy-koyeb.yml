name: Deploy to Koyeb
on: [push]

env:
  GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Docker build
        run: docker build --rm=false -t ghcr.io/matsukenbass/my-project:latest .
      - name: Docker login
        run: echo $GHCR_TOKEN | docker login ghcr.io -u matsukenbass --password-stdin
      - name: Docker push
        run: docker push ghcr.io/matsukenbass/my-project:latest
      - name: Install and configure the Koyeb CLI
        uses: koyeb-community/install-koyeb-cli@v2
        with:
          api_token: "${{ secrets.KOYEB_TOKEN }}"
          github_token: "${{ secrets.GITHUB_TOKEN }}"
      - name: Deploy to Koyeb
        run: koyeb service redeploy digital-ec/digital-ec
